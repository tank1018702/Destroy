namespace Destroy
{
    public enum NetworkRole
    {
        None,
        Client,
        Server,
        Host,
    }

    public enum GameState
    {
        Room,
        Game,
    }

    public enum RoomCommand
    {

    }

    public enum GameCommand
    {

    }
}